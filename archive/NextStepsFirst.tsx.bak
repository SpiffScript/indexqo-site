import React from "react";
import { Container } from "../ui/Container";

export const NextStepsFirst: React.FC = () => {
  return (
    <section className="bg-white py-16 md:py-20">
      <Container>
        <div className="border-t border-slate-100 pt-16 md:pt-20">
          {/* Section label */}
          <div className="mb-10 flex flex-col gap-2">
            <span className="text-xs tracking-widest uppercase text-slate-500">
              The signal review
            </span>
            <span className="h-px w-10 bg-slate-300" />
          </div>

          {/* Main grid */}
          <div className="grid grid-cols-1 items-start gap-12 lg:grid-cols-12 lg:gap-14">
            {/* Left: headline */}
            <div className="relative lg:col-span-7 pl-8">
              {/* Accent line */}
              <span className="absolute left-0 top-2 h-16 w-px bg-sky-600" />

              <h3 className="text-3xl font-semibold leading-tight tracking-tight text-slate-900 md:text-4xl">
                Every engagement begins the same way:
                <br />
                a signal review.
              </h3>
            </div>

            {/* Right: supporting copy */}
            <div className="lg:col-span-5 max-w-xl text-slate-700">
              <p className="text-[15px] leading-7 text-slate-600">
                Before any design, messaging, or implementation work is discussed, we examine how
                your business is currently represented across its public footprint.
              </p>
              <p className="mt-4 text-[15px] leading-7 text-slate-600">
                This step exists to establish clarity, not commitment. No work is proposed until
                the signal is understood.
              </p>
            </div>
          </div>
        </div>
      </Container>
    </section>
  );
};

export default NextStepsFirst;
